# secrets
.env

# large outputs live on $WORK; ./outputs is a symlink to them (a plain folder on a local copy)
/outputs

# environments and build artifacts
.venv/
dist/
build/
*.egg-info/

# caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Claude Code personal settings (the sbatch allow rule only means something on LS6)
.claude/settings.local.json
